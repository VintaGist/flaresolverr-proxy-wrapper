{
  "name": "flaresolverr-wrapper",
  "version": "1.0.0",
  "description": "FlareSolverr wrapper with dynamic proxy support",
  "main": "wrapper.js",
  "scripts": {
    "start": "node wrapper.js",
    "dev": "NODE_ENV=development nodemon wrapper.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "proxy-chain": "^2.4.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}